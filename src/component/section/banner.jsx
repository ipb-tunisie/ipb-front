import ImageSwiper from "./ImageSwiper";

const Banner = () => {
  return (
    <section
      className="hero__area hero__height hero__height-1 d-flex align-items-center  p-relative fix"
      style={{ padding: "6rem 0 0 0" }}
    >
      <ImageSwiper />
    </section>
  );
};

export default Banner;
